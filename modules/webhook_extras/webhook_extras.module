<?php

/**
 * Webhook extras module file.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function webhook_extras_ctools_plugin_directory($module, $type) {
  if ('webhook' == $module || 'export_ui' == $type) {
    return "plugins/{$type}";
  }
}
